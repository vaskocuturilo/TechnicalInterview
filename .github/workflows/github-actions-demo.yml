name: CI/CD Pipeline
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

    env:
      # Java
      JAVA_VERSION: 1.15
jobs:
  init:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Java JDK
        uses: actions/setup-java@v1
        with:
          java-version: '11'

      - name: Gradle init.
        run: gradle init

      - name: Create gradle wrapper.
        run: gradle wrapper

      - name: Build project.
        run: ./gradlew build

      - name: Run UI, API and Unit tests.
        run: ./gradlew test
